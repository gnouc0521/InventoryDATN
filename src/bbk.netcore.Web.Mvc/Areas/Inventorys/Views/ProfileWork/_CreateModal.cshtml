@using Abp.Localization
@using Abp.MultiTenancy
@using bbk.netcore.MultiTenancy


@{
    Layout = null;
}
<div class="modal-content" id="CustomerCreateModal">
    <div class="modal-header">
        <h5 class="modal-title">Thêm mới </h5><button type="button" class="close" data-dismiss="modal"
                                                      aria-label="Close">
            <span aria-hidden="true"><i class="fal fa-times"></i></span>
        </button>
    </div>
    <div class="modal-body">
        <form name="frmCreate" role="form" class="needs-validation" novalidate="">
            <div class="form-group row required">
                <label class="col-md-3 col-form-label">@(new FixedLocalizableString("Tiêu đề"))</label>
                <div class="col-md-9">
                    <input type="text" name="Title" class="form-control" required minlength="">
                </div>
            </div>
           @* <div class="form-group row required">
                <label class="col-md-3 col-form-label">@(new FixedLocalizableString("Mức"))</label>
                <div class="col-md-9">
                    <input type="text" name="ParentId" class="form-control" required minlength="">
                </div>
            </div>
            <div class="form-group row required">
                <label class="col-md-3 col-form-label">@(new FixedLocalizableString("Thứ tự"))</label>
                <div class="col-md-9">
                    <input type="text" name="Order" class="form-control" minlength="" required>
                </div>
            </div>
            <div class="form-group row required">
                <label class="col-md-3 col-form-label">@(new FixedLocalizableString("Người chủ trì"))</label>
                <div class="col-md-9">
                    <select class="select2-placeholder-multiple form-control" asp-for="WorkUser" required asp-items="ViewBag.Name">
                        <option value=""></option>
                    </select>
                </div>
            </div>*@
        </form>
    </div>
</div>
@await Html.PartialAsync("~/Areas/App/Views/Common/Modals/_ModalFooterWithSaveAndCancel_Custom.cshtml")


