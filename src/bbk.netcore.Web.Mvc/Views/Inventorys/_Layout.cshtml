@using Abp.Configuration.Startup
@using Abp.Web.Security.AntiForgery
@using bbk.netcore
@using bbk.netcore.Web.Resources
@inject IAbpAntiForgeryManager AbpAntiForgeryManager
@inject IWebResourceManager WebResourceManager
@inject ILanguageManager LanguageManager
@inject IMultiTenancyConfig MultiTenancyConfig
@{ Layout = null;
                AbpAntiForgeryManager.SetCookie(Context);

                var pageTitle = !string.IsNullOrWhiteSpace(ViewBag.Title) ? ViewBag.Title + " / " : "TCCB: Đăng nhập";
                pageTitle += "netcore"; }
<!DOCTYPE html>
<html lang="@LanguageManager.CurrentLanguage.Name">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no, user-scalable=no, minimal-ui">

    <title>@pageTitle</title>

    <meta name="description" content="TCCB Login">
    <!-- Call App Mode on ios devices -->
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <!-- Remove Tap Highlight on Windows Phone IE -->
    <meta name="msapplication-tap-highlight" content="no">

    <!-- base css -->
    @*<link rel="stylesheet" media="screen, print" href="css/vendors.bundle.css">
        <link rel="stylesheet" media="screen, print" href="css/app.bundle.css">*@

    @await Html.PartialAsync("_Styles.cshtml")

    <!-- Place favicon.ico in the root directory -->
    <link rel="apple-touch-icon" sizes="180x180" href="img/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="img/favicon/favicon-32x32.png">
    <link rel="mask-icon" href="img/favicon/safari-pinned-tab.svg" color="#5bbad5">
    <!-- Optional: page related CSS-->
    @*<link rel="stylesheet" media="screen, print" href="css/page-login.css">*@

    @RenderSection("styles", required: false)
</head>
<body>
    @RenderBody()

    @await Html.PartialAsync("_Scripts.cshtml")
    @RenderSection("scripts", required: false)
    @WebResourceManager.RenderScripts()
</body>
@*<body class="hold-transition login-page">
        <div class="login-box">
            <div class="login-logo">
                <a href="/"><b>netcore</b></a>
            </div>
            <div class="card">
                @if (MultiTenancyConfig.IsEnabled)
                {
                    <div class="card-header">
                        <vc:tenant-change></vc:tenant-change>
                    </div>
                }
                <div class="card-body login-card-body">
                    @RenderBody()
                </div>
                <div class="card-footer">
                    <vc:account-languages></vc:account-languages>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12 text-center">
                    <small>&copy; @DateTime.Now.Year BBK. <b>Version </b> @AppVersionHelper.Version [@AppVersionHelper.ReleaseDate.ToString("yyyyMMdd")]</small>
                </div>
            </div>
        </div>
        @await Html.PartialAsync("_Scripts.cshtml")
        @RenderSection("scripts", required: false)
        @WebResourceManager.RenderScripts()
    </body>*@
</html>
